# Core settings
OPENROUTER_API_KEY=your-openrouter-api-key
FRONTEND_URL=http://localhost:5173
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/api/google-auth/callback

# Optional: override the default model or OpenRouter endpoint
# OPENROUTER_DEFAULT_MODEL=openrouter/auto
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional provider keys (delete any you do not use)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
# MISTRAL_API_KEY=
# GROQ_API_KEY=
# LLAMA_API_KEY=
# GROK_API_KEY=

# Deepgram speech-to-text (optional for browser STT support)
# DEEPGRAM_API_KEY=
# DEEPGRAM_ALLOW_APIKEY_FALLBACK=false

# Attachments storage (defaults shown)
# ATTACHMENTS_DIR=data/uploads
# ATTACHMENTS_MAX_SIZE_BYTES=10485760
# ATTACHMENTS_RETENTION_DAYS=7

# Notes:
# 1. Copy this file to `.env` and fill in the secrets locally. `.env` is gitignored.
# 2. OAuth client JSON files belong in `credentials/` (also gitignored). Create a Web
#    client in Google Cloud Console with redirect URI
#    `http://localhost:8000/api/google-auth/callback` and download the client_secret file.
